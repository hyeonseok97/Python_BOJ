# 1로 만들기

n = int(input()) # 정수 n을 입력받음

d = [0]*(n+1) # 연산 횟수를 저장하기 위한 리스트 초기화

for i in range(2,n+1):
    d[i] = d[i-1] + 1 # 1을 빼는 경우
    if i % 3 == 0: # 3으로 나누어 떨어지는 경우
        d[i] = min(d[i//3]+1,d[i])
    if i%2 == 0: # 2로 나누어 떨어지는 경우
        d[i] = min(d[i//2]+1, d[i])

print(d[n])
